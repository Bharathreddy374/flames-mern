services:
  - type: web
    name: flames-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: mongodb://localhost:27017/flamesDB
        value: your_mongo_uri_here
    rootDir: server
